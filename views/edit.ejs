<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="h-screen w-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a href="/read" class="text-blue-500">Read Users</a>
        </div>
       
        <h1 class="flex justify-center text-3xl tracking-tighter">Update User</h1>
        <hr class="mb-5 mt-3">
        <form action="/update/<%=user._id %>" method="post" class="flex justify-center">
            <input class="px-5 py-2 mr-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" value="<%=user.name %>" name="name" type="text" placeholder="New Name">
            <input class="px-5 py-2 ml-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" value="<%=user.email%>" name="email" type="text" placeholder=" New Email">
            <input class="px-5 py-2 ml-3 border-2 border-zinc-800 rounded-lg outline-none bg-transparent" value="<%=user.image%>" name="image" type="text" placeholder=" New Image URL">
             <input class="px-5 py-2 bg-yellow-500 rounded-lg ml-3" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>